# تشغيل الرحلات التشغيلية الكاملة

## ترتيب SQL

1. خذ نسخة احتياطية من قاعدة البيانات.
2. شغّل `supabase/fees-and-provider-readiness.sql`.
3. شغّل `supabase/complete-operations.sql`.
4. من Supabase Cron تأكد من وجود مهمة `wujha-complete-inspections` كل 10 دقائق. إذا لم تُنشأ تلقائيًا، اجعلها تنفذ:
   `select public.complete_expired_inspections();`

## المسارات

- دخول الإدارة الخاص: `/admin-login/`
- لوحات المستخدمين الحالية تتوسع تلقائيًا بحسب الدور.
- التسليم وطلب الصرف يظهران داخل صفحة الصفقة.
- الأدلة ترفع إلى bucket خاص باسم `dispute-evidence` بروابط موقعة مؤقتة.

## حماية الأموال

- طلب الصرف لا يتحول إلى رصيد إلا إذا وجدت دفعة `paid` موثقة للصفقة.
- تسجيل السحب كمدفوع يتطلب مرجع تحويل بنكي.
- حاليًا لا توجد شركة دفع مرتبطة، ولذلك ستظهر طلبات الصرف بحالة انتظار مزود الدفع، وهذا مقصود.
- بعد التعاقد يلزم Edge Function سري لإنشاء الدفع وWebhook موقّع لإضافة حالة `paid`. لا تضع مفاتيح المزود داخل ملفات المتصفح.

## الفواتير والضريبة

- ينشأ رقم مستند تسلسلي عند اكتمال الصفقة.
- المنشأة غير مسجلة في ضريبة القيمة المضافة حاليًا؛ لذلك يصدر مستند غير ضريبي وVAT يساوي صفرًا.
- لا تفعّل `vat_enabled` إلا بعد التسجيل وإضافة الرقم الضريبي ومراجعة محاسب/مستشار ضريبي.
