const A = 'rounded-2xl border border-slate-200 bg-white shadow-sm';
const money = n => new Intl.NumberFormat('ar-SA', { style: 'currency', currency: 'SAR' }).format(n);
const base = document.body.dataset.base || '';
const escapeHtml = value => String(value ?? '').replace(/[&<>'"]/g, char => ({ '&':'&amp;', '<':'&lt;', '>':'&gt;', "'":'&#39;', '"':'&quot;' })[char]);
const normalizePhone = value => {
  const digits = String(value || '').replace(/\D/g, '');
  if (/^5\d{8}$/.test(digits)) return `+966${digits}`;
  if (/^05\d{8}$/.test(digits)) return `+966${digits.slice(1)}`;
  if (/^9665\d{8}$/.test(digits)) return `+${digits}`;
  return String(value || '').trim();
};
const setupSaudiPhoneInputs = () => {
  document.querySelectorAll('input[name="phone"],input[name="counterparty_phone"]').forEach(input => {
    if (input.readOnly || input.dataset.saudiPhoneReady) return;
    input.dataset.saudiPhoneReady = 'true';
    input.type = 'tel';
    input.inputMode = 'numeric';
    input.pattern = '5[0-9]{8}';
    input.minLength = 9;
    input.maxLength = 9;
    input.placeholder = '5XXXXXXXX';
    input.autocomplete = 'tel-national';
    input.className = 'min-w-0 flex-1 bg-transparent px-3 py-3 outline-none';
    const wrapper = document.createElement('div');
    wrapper.dir = 'ltr';
    wrapper.className = 'mt-2 flex w-full items-center overflow-hidden rounded-xl border bg-white focus-within:ring-2 focus-within:ring-emerald-600';
    input.parentNode.insertBefore(wrapper, input);
    wrapper.append(input);
    const prefix = document.createElement('span');
    prefix.textContent = '+966';
    prefix.className = 'select-none border-r bg-slate-100 px-3 py-3 font-bold text-slate-700';
    prefix.setAttribute('aria-hidden', 'true');
    wrapper.prepend(prefix);
  });
};
const setBusy = (button, busy, busyText = 'جارٍ التنفيذ...') => {
  if (!button) return;
  if (busy) button.dataset.originalText = button.textContent;
  button.disabled = busy;
  button.classList.toggle('opacity-60', busy);
  button.textContent = busy ? busyText : (button.dataset.originalText || button.textContent);
};
const pages = {
login: `<section class="mx-auto max-w-md ${A} p-7"><p class="text-sm font-bold text-emerald-700">مرحباً بعودتك</p><h1 class="mt-2 text-3xl font-black">تسجيل الدخول</h1><form id="loginForm" class="mt-7 space-y-4"><label class="block text-sm font-bold">رقم الجوال<input name="phone" dir="ltr" required pattern="\\+9665[0-9]{8}" placeholder="+9665xxxxxxxx" class="mt-2 w-full rounded-xl border p-3"></label><label class="block text-sm font-bold">كلمة المرور<input name="password" type="password" minlength="8" required class="mt-2 w-full rounded-xl border p-3"></label><button class="w-full rounded-xl bg-emerald-700 p-3 font-bold text-white">دخول آمن</button><p id="formMessage" class="text-sm"></p></form><button id="forgotPassword" class="mt-4 w-full text-sm font-bold text-emerald-700">نسيت كلمة المرور؟</button><p class="mt-5 text-center text-sm">ليس لديك حساب؟ <a class="font-bold text-emerald-700" href="${base}register/">أنشئ حساباً</a></p></section>`,
register: `<section class="mx-auto max-w-xl ${A} p-7"><p class="text-sm font-bold text-emerald-700">انضم إلى وجهة</p><h1 class="mt-2 text-3xl font-black">إنشاء حساب موثّق</h1><form id="registerForm" class="mt-7 grid gap-4 sm:grid-cols-2"><label class="text-sm font-bold">الاسم الكامل<input name="name" required class="mt-2 w-full rounded-xl border p-3"></label><label class="text-sm font-bold">الجوال<input name="phone" dir="ltr" required pattern="\\+9665[0-9]{8}" placeholder="+9665xxxxxxxx" class="mt-2 w-full rounded-xl border p-3"></label><label class="text-sm font-bold">البريد الإلكتروني<input name="email" type="email" dir="ltr" class="mt-2 w-full rounded-xl border p-3"></label><label class="text-sm font-bold">نوع الحساب<select name="userType" id="userType" class="mt-2 w-full rounded-xl border p-3"><option value="buyer">مشتري</option><option value="seller">بائع / تاجر</option></select></label><label id="crField" class="hidden text-sm font-bold sm:col-span-2">رقم السجل التجاري<input name="commercialRegister" minlength="10" maxlength="15" class="mt-2 w-full rounded-xl border p-3"></label><label class="text-sm font-bold sm:col-span-2">كلمة المرور<input name="password" type="password" minlength="8" required class="mt-2 w-full rounded-xl border p-3"></label><button class="rounded-xl bg-emerald-700 p-3 font-bold text-white sm:col-span-2">إنشاء الحساب</button><p id="formMessage" class="text-sm sm:col-span-2"></p></form></section>`,
marketplace: `<div class="flex flex-wrap items-end justify-between gap-4"><div><p class="font-bold text-emerald-700">سوق وجهة المفتوح</p><h1 class="text-3xl font-black">تسوّق بثقة، وادفع بضمان</h1></div><button id="showProductForm" class="hidden rounded-xl bg-amber-400 px-5 py-3 font-black">+ إضافة منتج</button></div><form id="productForm" class="mt-6 hidden grid gap-3 ${A} p-5 sm:grid-cols-2"><input name="title" required minlength="3" maxlength="160" placeholder="عنوان المنتج" class="rounded-xl border p-3"><input name="price" required min="1" step="0.01" type="number" placeholder="السعر" class="rounded-xl border p-3"><select name="category" required class="rounded-xl border p-3"><option>قطع غيار السيارات والنقل الثقيل</option><option>أجهزة وإلكترونيات وهواتف</option><option>أثاث منزلي</option><option>خدمات تقنية وتسويقية نيابة عن الغير</option></select><select name="condition" class="rounded-xl border p-3"><option value="new">جديد</option><option value="used">مستعمل</option></select><input name="location" placeholder="الموقع" class="rounded-xl border p-3"><input name="vin" placeholder="VIN اختياري" class="rounded-xl border p-3"><input name="main_image" type="url" placeholder="رابط الصورة" class="rounded-xl border p-3 sm:col-span-2"><textarea name="description" required placeholder="الوصف" class="rounded-xl border p-3 sm:col-span-2"></textarea><button class="rounded-xl bg-emerald-700 p-3 font-bold text-white sm:col-span-2">نشر المنتج</button><p id="productMessage" class="text-sm sm:col-span-2"></p></form><section class="mt-6 ${A} p-4"><div class="grid gap-3 md:grid-cols-[1fr_auto]"><input id="marketSearch" placeholder="ابحث بكلمة أو رقم الهيكل VIN" class="rounded-xl border p-3"><div id="filters" class="flex flex-wrap gap-2">${['الكل','قطع غيار','أجهزة وإلكترونيات','أثاث منزلي','خدمات تقنية وتسويق'].map((x,i)=>`<button data-filter="${x}" class="rounded-full px-4 py-2 text-sm font-bold ${i?'bg-slate-100':'bg-emerald-700 text-white'}">${x}</button>`).join('')}</div></div></section><div id="productGrid" class="mt-6 grid gap-5 sm:grid-cols-2 lg:grid-cols-3"></div>`,
createDeal: `<div class="mx-auto max-w-3xl"><p class="font-bold text-emerald-700">صفقة جديدة</p><h1 class="text-3xl font-black">أنشئ معاملتك الآمنة</h1><div class="mt-5 flex gap-2">${[1,2,3,4,5,6].map(n=>`<span class="step-dot h-2 flex-1 rounded-full ${n===1?'bg-emerald-700':'bg-slate-200'}"></span>`).join('')}</div><form id="dealWizard" class="mt-6 ${A} p-7"><div data-step="1"><h2 class="text-xl font-black">ما دورك في الصفقة؟</h2><div class="mt-4 grid gap-3 sm:grid-cols-2"><label class="cursor-pointer rounded-xl border p-4"><input type="radio" name="role" value="seller" required> بائع / مقدم خدمة</label><label class="cursor-pointer rounded-xl border p-4"><input type="radio" name="role" value="buyer"> مشتري</label></div></div><div data-step="2" class="hidden space-y-4"><h2 class="text-xl font-black">بيانات الطرف الآخر</h2><input name="counterparty_phone" dir="ltr" required pattern="\\+9665[0-9]{8}" placeholder="جوال الطرف الآخر: +9665xxxxxxxx" class="w-full rounded-xl border p-3"><input name="external_url" type="url" placeholder="رابط الإعلان الخارجي (اختياري)" class="w-full rounded-xl border p-3"><p class="text-xs text-slate-500">يجب أن يكون الطرف الآخر مسجلًا في وجهة، وألا يكون هو حسابك نفسه.</p></div><div data-step="3" class="hidden"><h2 class="text-xl font-black">التصنيف المعتمد</h2><select name="category" required class="mt-4 w-full rounded-xl border p-3"><option>قطع غيار السيارات والنقل الثقيل</option><option>أجهزة وإلكترونيات وهواتف</option><option>أثاث منزلي</option><option>خدمات تقنية وتسويقية نيابة عن الغير</option></select></div><div data-step="4" class="hidden space-y-4"><h2 class="text-xl font-black">القيمة وشروط المعاينة</h2><input name="deal_price" type="number" min="1" step="0.01" required placeholder="قيمة المعاملة ر.س" class="w-full rounded-xl border p-3"><select name="fee_bearer" class="w-full rounded-xl border p-3"><option value="buyer">المشتري يتحمل الرسوم</option><option value="seller">البائع يتحمل الرسوم</option><option value="split">مناصفة</option></select><select name="inspection_days" class="w-full rounded-xl border p-3"><option value="1">24 ساعة</option><option value="2">48 ساعة</option><option value="3">3 أيام</option><option value="7">7 أيام</option></select></div><div data-step="5" class="hidden"><h2 class="text-xl font-black">خيارات التقسيط</h2><label class="mt-4 flex items-center justify-between rounded-xl bg-slate-50 p-4"><span>طلب إتاحة التقسيط عند توفر بوابة الدفع</span><input name="installments" type="checkbox" class="h-6 w-6 accent-emerald-700"></label><p id="dealMessage" class="mt-4 text-sm"></p></div><div data-step="6" class="hidden text-center"><div class="mx-auto grid h-16 w-16 place-items-center rounded-full bg-emerald-100 text-3xl text-emerald-700">✓</div><h2 class="mt-4 text-2xl font-black">تم إنشاء الصفقة</h2><p id="dealSummary" class="mt-2 text-slate-600"></p><div class="mt-5 flex flex-wrap justify-center gap-3"><a id="waShare" target="_blank" rel="noopener" class="rounded-xl bg-emerald-600 px-5 py-3 font-bold text-white">مشاركة عبر WhatsApp</a><button id="copyDeal" type="button" class="rounded-xl border px-5 py-3 font-bold">نسخ الرابط</button><a id="openDeal" class="rounded-xl border px-5 py-3 font-bold">فتح الصفقة</a></div></div><div id="wizardActions" class="mt-7 flex justify-between"><button type="button" id="prevStep" class="invisible rounded-xl border px-5 py-2">السابق</button><button type="button" id="nextStep" class="rounded-xl bg-emerald-700 px-5 py-2 font-bold text-white">التالي</button></div></form></div>`,
dealView: `<div id="dealContent" class="${A} p-6">جارٍ تحميل الصفقة الحقيقية...</div>`,
productDetail: `<div id="productDetailContent" class="${A} p-6">جارٍ تحميل المنتج...</div>`,
buyer: dashboard('لوحة المشتري','صفقاتك وطلباتك المحمية'),
seller: dashboard('لوحة البائع','أدر عروضك وتحصيلاتك'),
admin: dashboard('لوحة الإدارة','رقابة تشغيلية وصلاحيات حصرية'),
dispute: `<section class="${A} p-6"><h1 class="text-3xl font-black">مركز النزاعات</h1><form id="disputeForm" class="mt-5 space-y-4"><textarea name="reason" required minlength="10" rows="5" placeholder="اشرح سبب الاعتراض بالتفصيل" class="w-full rounded-xl border p-3"></textarea><input name="attachments" placeholder="روابط الأدلة، كل رابط في سطر" class="w-full rounded-xl border p-3"><button class="rounded-xl bg-red-700 px-5 py-3 font-bold text-white">فتح النزاع</button><p id="disputeMessage" class="text-sm"></p></form><div id="disputeList" class="mt-6"></div></section>`,
invoice: `<section id="invoiceContent" class="mx-auto max-w-3xl ${A} p-8">جارٍ تحميل الفاتورة...</section>`,
profile: `<div><p class="font-bold text-emerald-700">الحساب</p><h1 class="text-3xl font-black">الملف الشخصي والتوثيق</h1></div><form id="profileForm" class="mt-6 grid gap-5 ${A} p-6 sm:grid-cols-2"><label class="text-sm font-bold">الاسم الكامل<input name="name" required class="mt-2 w-full rounded-xl border p-3"></label><label class="text-sm font-bold">رقم الجوال<input name="phone" readonly class="mt-2 w-full rounded-xl border bg-slate-100 p-3"></label><label class="text-sm font-bold">البريد الإلكتروني<input name="email" type="email" class="mt-2 w-full rounded-xl border p-3"></label><label class="text-sm font-bold">رقم الهوية الوطنية<input name="national_id" class="mt-2 w-full rounded-xl border p-3"></label><label class="text-sm font-bold">رقم السجل التجاري<input name="commercial_register" class="mt-2 w-full rounded-xl border p-3"></label><label class="text-sm font-bold sm:col-span-2">الحساب البنكي IBAN<input name="iban_bank" dir="ltr" pattern="SA[0-9]{22}" class="mt-2 w-full rounded-xl border p-3"></label><button class="rounded-xl bg-emerald-700 p-3 font-bold text-white sm:col-span-2">حفظ البيانات</button><p id="profileMessage" class="text-sm sm:col-span-2"></p></form><form id="passwordForm" class="mt-6 ${A} p-6"><h2 class="text-xl font-black">تغيير كلمة المرور</h2><input name="password" type="password" minlength="8" required placeholder="كلمة المرور الجديدة" class="mt-4 w-full rounded-xl border p-3"><button class="mt-3 rounded-xl bg-slate-900 px-5 py-3 font-bold text-white">تحديث كلمة المرور</button><p id="passwordMessage" class="mt-2 text-sm"></p></form><section class="mt-6 rounded-2xl border border-red-200 bg-white p-6"><h2 class="text-xl font-black text-red-700">حذف الحساب</h2><p class="mt-2 text-sm text-slate-600">يرسل الطلب للإدارة للمراجعة. لا يُحذف الحساب تلقائيًا لأن السجلات المالية والنظامية قد تتطلب الاحتفاظ.</p><form id="deleteAccountForm" class="mt-4 flex flex-col gap-3 sm:flex-row"><input name="reason" required minlength="5" placeholder="سبب طلب الحذف" class="flex-1 rounded-xl border p-3"><button class="rounded-xl bg-red-700 px-5 py-3 font-bold text-white">طلب حذف الحساب</button></form><p id="deleteMessage" class="mt-2 text-sm"></p></section>`,
privacy: legal('سياسة الخصوصية',[['البيانات التي نجمعها','بيانات الهوية والتواصل والتوثيق والسجل التجاري والحساب البنكي وبيانات المعاملات والمرفقات اللازمة لتقديم الخدمة.'],['أغراض المعالجة','إنشاء الحساب، تنفيذ الوساطة والضمان، مكافحة الاحتيال، الامتثال، وإرسال الإشعارات التشغيلية.'],['المشاركة والحفظ','لا نشارك البيانات إلا مع مزودي الدفع والجهات النظامية ومقدمي الخدمة بالقدر اللازم، ونحتفظ بها وفق الالتزامات النظامية.'],['حقوقك','يمكنك طلب الوصول أو التصحيح أو الاستفسار عبر الرقم الرسمي +966555264930.']]),
terms: legal('شروط الاستخدام',[['نطاق المنصة','وجهة وسيط تقني وتجاري وليست مالكة للسلع المعروضة، ويتحمل المعلن مسؤولية صحة وصفه وملكيته.'],['الحسابات','يلتزم المستخدم بتقديم بيانات صحيحة وحماية بيانات الدخول، ويخضع حساب الأدمن لصلاحية داخلية فقط.'],['الرسوم','رسوم الوساطة 2.5% بحد أدنى 50 ر.س ما لم تظهر شروط خاصة قبل اعتماد الصفقة، وتطبق الضريبة على الرسوم فقط.'],['الممنوعات','يحظر عرض السلع غير النظامية أو التحايل على الضمان أو تقديم مستندات مضللة.']]),
escrow: legal('اتفاقية الوساطة والضمان',[['حجز المبلغ','يودع المشتري المبلغ لدى مسار الدفع المخصص للصفقة ويبقى محجوزاً حتى تحقق شروط التحرير.'],['المعاينة والاستلام','تبدأ المهلة بعد تأكيد التسليم، ولا يتاح تأكيد الاستلام قبل مرور 24 ساعة، وتمتد المهلة المتفق عليها حتى 7 أيام.'],['النزاعات','يجوز فتح اعتراض وإرفاق الأدلة خلال 48 ساعة، ويجوز للأدمن رد المبلغ أو تحريره أو تقسيمه وفق الأدلة.'],['الإخلاء والاعتماد','اعتماد الصفقة إلكترونياً موافقة على هذه الاتفاقية وعلى احتساب الرسوم والضريبة المبينة قبل الدفع.']])
};

function dashboard(title, sub){return `<div class="flex flex-wrap justify-between gap-3"><div><p class="font-bold text-emerald-700">${sub}</p><h1 class="text-3xl font-black">${title}</h1></div><a href="${base}create-deal/" class="rounded-xl bg-amber-400 px-5 py-3 font-black">+ إنشاء صفقة</a></div><section class="mt-6 ${A} p-6"><h2 class="text-xl font-black">بيانات الحساب</h2><div id="dashboardContent" class="mt-4 text-slate-600">جارٍ تحميل بياناتك...</div></section>`}
function legal(title, sections){return `<article class="mx-auto max-w-4xl ${A} p-8"><p class="font-bold text-emerald-700">آخر تحديث: 07 أغسطس 2026</p><h1 class="mt-2 text-3xl font-black">${title}</h1><div class="mt-7 space-y-7">${sections.map((s,i)=>`<section><h2 class="text-xl font-black">${i+1}. ${s[0]}</h2><p class="mt-2 leading-8 text-slate-600">${s[1]}</p></section>`).join('')}</div><div class="mt-8 rounded-xl bg-slate-50 p-4 text-sm">الجهة المشغلة: مؤسسة يوسف عيد المطيري لقطع غيار السيارات والخدمات التجارية — س.ت 7054534024</div></article>`}

function shell(content){return `<header class="border-b bg-white"><nav class="mx-auto flex max-w-7xl items-center justify-between gap-4 px-5 py-4"><a href="${base}" class="text-2xl font-black text-emerald-800">وجهة<span class="text-amber-500">.</span></a><div class="hidden items-center gap-5 text-sm font-bold md:flex"><a href="${base}marketplace/">السوق</a><a href="${base}create-deal/">إنشاء صفقة</a><a id="dashboardLink" href="${base}dashboard-buyer/">صفقاتي</a><a href="${base}profile/">حسابي</a></div><button id="authAction" class="rounded-xl bg-emerald-700 px-4 py-2 text-sm font-bold text-white">جارٍ التحقق...</button></nav></header><main class="mx-auto min-h-[75vh] max-w-7xl px-5 py-10">${content}</main><footer class="mt-10 bg-slate-950 text-slate-300"><div class="mx-auto grid max-w-7xl gap-6 px-5 py-8 md:grid-cols-3"><div><p class="text-xl font-black text-white">وجهة</p><p class="mt-2 text-sm">وساطة وضمان تجاري وسوق مفتوح سعودي.</p></div><div class="text-sm"><p>مؤسسة يوسف عيد المطيري لقطع غيار السيارات والخدمات التجارية</p><p>س.ت: 7054534024</p><a dir="ltr" href="https://wa.me/966555264930">+966555264930</a></div><div class="flex flex-col text-sm"><a href="${base}privacy-policy/">سياسة الخصوصية</a><a href="${base}terms-of-use/">شروط الاستخدام</a><a href="${base}escrow-agreement/">اتفاقية الوساطة والضمان</a></div></div></footer>`}

const page = document.body.dataset.page;
document.getElementById('app').innerHTML = shell(pages[page] || '<h1>الصفحة غير موجودة</h1>');
if(document.body.dataset.adminLogin==='true')setupAdminLoginForm();
if(page==='createDeal'){
  const step=document.querySelector('[data-step="2"]');
  const phone=step.querySelector('input[name="counterparty_phone"]');
  const names=document.createElement('div');
  names.className='grid gap-3 sm:grid-cols-2';
  names.innerHTML='<input name="invitee_first_name" minlength="2" placeholder="الاسم الأول (مطلوب إذا لم يكن مسجلًا)" class="rounded-xl border p-3"><input name="invitee_last_name" minlength="2" placeholder="الاسم الأخير (مطلوب إذا لم يكن مسجلًا)" class="rounded-xl border p-3">';
  step.insertBefore(names,phone);
}
setupSaudiPhoneInputs();
if(page==='register'){
  const query=new URLSearchParams(location.search),form=document.getElementById('registerForm');
  if(query.get('phone'))form.elements.phone.value=query.get('phone').replace(/\D/g,'').slice(-9);
  if(query.get('name'))form.elements.name.value=query.get('name');
  if(['buyer','seller'].includes(query.get('role'))){
    form.elements.userType.value=query.get('role');
    const seller=query.get('role')==='seller',field=document.getElementById('crField');
    field.classList.toggle('hidden',!seller);
    field.querySelector('input').required=seller;
  }
}

if(page==='marketplace'){initMarket();document.getElementById('productGrid').addEventListener('click',e=>{const link=e.target.closest('a[href*="create-deal/?product="]');if(!link)return;e.preventDefault();const id=new URL(link.href).searchParams.get('product');location.href=`${base}product-details/?id=${encodeURIComponent(id)}`;});}
if(page==='productDetail') initProductDetail();
if(page==='createDeal') initWizard();
if(page==='createDeal') setupFeePreview();
applyCommercialCopy();
if(page==='register') document.getElementById('userType').addEventListener('change',e=>{const field=document.getElementById('crField'),input=field.querySelector('input'),seller=e.target.value==='seller';field.classList.toggle('hidden',!seller);input.required=seller;});
initAuth();

async function initProductDetail(){const el=document.getElementById('productDetailContent'),id=new URLSearchParams(location.search).get('id');if(!id){el.textContent='رقم المنتج مفقود.';return;}const api=await import('./supabase-client.js'),[{data:p,error},{data:images},{data:reviews}]=await Promise.all([api.products.get(id),api.products.images(id),api.products.reviews(id)]);if(error){el.textContent=error.message;return;}const avg=reviews?.length?(reviews.reduce((s,r)=>s+r.rating,0)/reviews.length).toFixed(1):'0.0',main=images?.[0]?.public_url||p.main_image;el.innerHTML=`<nav class="text-sm text-slate-500"><a href="${base}">الرئيسية</a> / <a href="${base}marketplace/">المنتجات</a> / ${escapeHtml(p.title)}</nav><div class="mt-6 grid gap-8 lg:grid-cols-2"><div>${main?`<img id="mainProductImage" src="${escapeHtml(main)}" class="h-96 w-full rounded-2xl object-cover">`:''}<div class="mt-3 flex gap-2 overflow-auto">${(images||[]).map(i=>`<button data-image="${escapeHtml(i.public_url)}"><img src="${escapeHtml(i.public_url)}" class="h-20 w-20 rounded-xl object-cover"></button>`).join('')}</div></div><div><span class="text-emerald-700">${escapeHtml(p.category)}</span><p class="mt-2 text-sm text-slate-500">${escapeHtml(p.sku||p.id.slice(0,8))}</p><h1 class="mt-2 text-3xl font-black">${escapeHtml(p.title)}</h1><p class="mt-3">★ ${avg} (${reviews?.length||0} تقييم)</p><p class="mt-5 text-3xl font-black">${money(p.price)}</p><p class="mt-2 font-bold text-emerald-700">${p.stock>0?`متوفر · ${p.stock} قطعة`:'غير متوفر'}</p><p class="mt-3 text-sm">${escapeHtml([p.region?.name_ar,p.city?.name_ar,p.district?.name_ar].filter(Boolean).join('، '))}</p><div class="mt-5 flex gap-3"><input id="productQty" type="number" min="1" max="${p.stock}" value="1" class="w-20 rounded-xl border p-3"><a href="${base}create-deal/?product=${p.id}" class="rounded-xl bg-emerald-700 px-6 py-3 font-bold text-white">شراء بضمان وجهة</a><button id="favoriteProduct" class="rounded-xl border px-4">♡</button></div><a class="mt-6 block rounded-xl bg-slate-50 p-4 font-bold" href="#">${escapeHtml(p.seller?.name||'متجر البائع')}</a></div></div><section class="mt-8"><h2 class="text-xl font-black">الوصف</h2><p class="mt-3 whitespace-pre-line">${escapeHtml(p.description)}</p><h2 class="mt-7 text-xl font-black">التقييمات (${reviews?.length||0})</h2><div class="mt-3 divide-y">${reviews?.length?reviews.map(r=>`<article class="py-3"><strong>${escapeHtml(r.reviewer?.name)}</strong><span class="mr-3">${'★'.repeat(r.rating)}</span><p>${escapeHtml(r.comment)}</p></article>`).join(''):'لا توجد تقييمات بعد.'}</div></section>`;el.onclick=e=>{if(e.target.closest('[data-image]'))document.getElementById('mainProductImage').src=e.target.closest('[data-image]').dataset.image;};}

async function initMarket(){const grid=document.getElementById('productGrid');grid.innerHTML='<p>جارٍ تحميل المنتجات...</p>';try{const {db}=await import('./supabase-client.js');const {data,error}=await db.products();if(error)throw error;const products=data||[];let filter='الكل';const render=()=>{const q=document.getElementById('marketSearch').value.trim().toLowerCase();grid.innerHTML=products.filter(p=>(filter==='الكل'||p.category.includes(filter))&&(!q||[p.title,p.description,p.vin,p.location].join(' ').toLowerCase().includes(q))).map(p=>`<article class="overflow-hidden ${A}">${p.main_image?`<img class="h-48 w-full object-cover" src="${escapeHtml(p.main_image)}" alt="${escapeHtml(p.title)}">`:''}<div class="p-5"><span class="rounded-full bg-emerald-50 px-3 py-1 text-xs font-bold text-emerald-700">${p.is_guaranteed?'✓ مشمول بضمان وجهة':'إعلان مباشر'}</span><h2 class="mt-3 text-lg font-black">${escapeHtml(p.title)}</h2><p class="mt-1 text-sm text-slate-500">${escapeHtml(p.category)} · ${escapeHtml(p.location||'غير محدد')}</p><p class="mt-3 text-xl font-black">${money(p.price)}</p><a href="${base}create-deal/?product=${encodeURIComponent(p.id)}" class="mt-4 block rounded-xl bg-emerald-700 p-3 text-center font-bold text-white">شراء بضمان المنصة</a></div></article>`).join('')||'<p class="rounded-xl bg-white p-6">لا توجد منتجات حقيقية منشورة حاليًا.</p>'};render();document.getElementById('marketSearch').oninput=render;document.getElementById('filters').onclick=e=>{if(!e.target.dataset.filter)return;filter=e.target.dataset.filter;[...e.currentTarget.children].forEach(b=>b.className='rounded-full bg-slate-100 px-4 py-2 text-sm font-bold');e.target.className='rounded-full bg-emerald-700 px-4 py-2 text-sm font-bold text-white';render()}}catch(err){grid.innerHTML=`<p class="text-red-700">تعذر تحميل السوق: ${escapeHtml(err.message)}</p>`}}
function initWizard(){
  let step=1;
  const form=document.getElementById('dealWizard');
  const next=document.getElementById('nextStep');
  const previous=document.getElementById('prevStep');
  const message=document.getElementById('dealMessage');
  const show=()=>{
    form.querySelectorAll('[data-step]').forEach(section=>section.classList.toggle('hidden',Number(section.dataset.step)!==step));
    document.querySelectorAll('.step-dot').forEach((dot,index)=>dot.className=`step-dot h-2 flex-1 rounded-full ${index<step?'bg-emerald-700':'bg-slate-200'}`);
    previous.classList.toggle('invisible',step===1);
    document.getElementById('wizardActions').classList.toggle('hidden',step===6);
    next.textContent=step===5?'إنشاء الصفقة':'التالي';
  };
  const validateStep=()=>{
    const fields=[...form.querySelector(`[data-step="${step}"]`).querySelectorAll('input,select')];
    for(const field of fields){if(!field.checkValidity()){field.reportValidity();return false;}}
    return true;
  };
  next.onclick=async()=>{
    if(!validateStep())return;
    if(step<5){step++;show();return;}
    setBusy(next,true,'جارٍ إنشاء الصفقة...');
    message.textContent='';
    try{
      const api=await import('./supabase-client.js');
      const {data:sessionData}=await api.getSession();
      const user=sessionData.session?.user;
      if(!user)throw new Error('انتهت جلسة الدخول. سجل الدخول مرة أخرى.');
      const values=Object.fromEntries(new FormData(form));
      const phone=normalizePhone(values.counterparty_phone);
      const {data:matches,error:lookupError}=await api.findDealCounterparty(phone);
      if(lookupError)throw lookupError;
      const counterparty=matches?.[0];
      const inviteeName=`${values.invitee_first_name} ${values.invitee_last_name}`.trim();
      if(!counterparty){
        if(String(values.invitee_first_name||'').trim().length<2||String(values.invitee_last_name||'').trim().length<2)throw new Error('العميل غير مسجل؛ أدخل الاسم الأول والأخير لإرسال الدعوة.');
        const {data:invitation,error:invitationError}=await api.invitations.create({inviteePhone:phone,inviteeName,creatorRole:values.role,externalUrl:values.external_url,category:values.category,feeBearer:values.fee_bearer,installments:values.installments==='on',dealPrice:Number(values.deal_price),inspectionHours:Number(values.inspection_days)*24});
        if(invitationError)throw invitationError;
        const expectedRole=values.role==='buyer'?'seller':'buyer';
        const registerUrl=new URL(`${base}register/`,location.href);
        registerUrl.searchParams.set('invite',invitation.token);
        registerUrl.searchParams.set('phone',phone);
        registerUrl.searchParams.set('name',inviteeName);
        registerUrl.searchParams.set('role',expectedRole);
        const shareText=`مرحبًا ${inviteeName}، لديك طلب صفقة على منصة وجهة. سجل حسابك بالرقم المدعو لقبول الصفقة: ${registerUrl.href}`;
        document.getElementById('dealSummary').textContent=`تم إنشاء دعوة معلقة باسم ${inviteeName}. ستتحول إلى صفقة تلقائيًا بعد تسجيل العميل.`;
        document.getElementById('waShare').href=`https://wa.me/${phone.replace(/\D/g,'')}?text=${encodeURIComponent(shareText)}`;
        document.getElementById('openDeal').href=registerUrl.href;
        document.getElementById('openDeal').textContent='فتح رابط التسجيل';
        document.getElementById('copyDeal').onclick=async()=>{await navigator.clipboard.writeText(registerUrl.href);document.getElementById('copyDeal').textContent='تم النسخ';};
        step=6;show();return;
      }
      if(values.role==='buyer'&&counterparty.user_type!=='seller')throw new Error('عند اختيار دور المشتري يجب أن يكون الطرف الآخر مسجلًا كبائع.');
      if(values.role==='seller'&&counterparty.user_type!=='buyer')throw new Error('عند اختيار دور البائع يجب أن يكون الطرف الآخر مسجلًا كمشتري.');
      const payload={
        product_id:new URLSearchParams(location.search).get('product')||null,
        buyer_id:values.role==='buyer'?user.id:counterparty.id,
        seller_id:values.role==='seller'?user.id:counterparty.id,
        external_ad_url:values.external_url||null,
        category:values.category,
        fee_bearer:values.fee_bearer,
        installment_enabled:values.installments==='on',
        deal_price:Number(values.deal_price),
        inspection_hours:Number(values.inspection_days)*24
      };
      const {data:deal,error}=await api.db.createDeal(payload);
      if(error)throw error;
      const url=new URL(`${base}deal-view/?id=${deal.id}`,location.href).href;
      document.getElementById('dealSummary').textContent=`الطرف الآخر: ${counterparty.name} — القيمة: ${money(deal.deal_price)} — الإجمالي: ${money(deal.total_amount_paid)}`;
      document.getElementById('waShare').href=`https://wa.me/?text=${encodeURIComponent('راجع صفقة وجهة الآمنة: '+url)}`;
      document.getElementById('openDeal').href=url;
      document.getElementById('copyDeal').onclick=async()=>{await navigator.clipboard.writeText(url);document.getElementById('copyDeal').textContent='تم النسخ';};
      step=6;show();
    }catch(error){message.textContent=error.message||'تعذر إنشاء الصفقة.';message.className='mt-4 text-sm text-red-700';}
    finally{setBusy(next,false);}
  };
  previous.onclick=()=>{step=Math.max(1,step-1);show();};
}

const rolePath=role=>role==='seller'?'dashboard-seller/':role==='admin'?'dashboard-admin/':'dashboard-buyer/';
async function initAuth(){
  const action=document.getElementById('authAction'),dashboardLink=document.getElementById('dashboardLink');
  let api,data;
  try{api=await import('./supabase-client.js');({data}=await api.getSession());}catch(error){action.textContent='تعذر الاتصال';action.title=error.message;return;}
  const session=data.session;
  let role=session?.user?.user_metadata?.user_type||'buyer';
  if(session){const {data:profile}=await api.profiles.mine();if(profile?.user_type)role=profile.user_type;if(profile&&profile.account_status&&profile.account_status!=='active'&&role!=='admin'){await api.signOut();action.textContent='الحساب موقوف';document.getElementById('app').innerHTML=shell('<section class="mx-auto max-w-xl rounded-2xl border border-red-200 bg-white p-7"><h1 class="text-2xl font-black text-red-700">الحساب غير نشط</h1><p class="mt-3">راجع الدعم لمعرفة سبب التعليق أو الحظر.</p></section>');return;}}
  if(dashboardLink)dashboardLink.href=`${base}${rolePath(role)}`;
  if(session){action.textContent='تسجيل خروج';action.onclick=async()=>{await api.signOut();location.href=base||'./'};}
  else{action.textContent='دخول';action.onclick=()=>location.href=`${base}login/`;}
  const protectedPages={buyer:['buyer'],seller:['seller'],admin:['admin'],profile:[],createDeal:[],dealView:[],dispute:[],invoice:[]};
  if(page in protectedPages){if(!session){location.href=`${base}login/?next=${encodeURIComponent(location.href)}`;return;}const roles=protectedPages[page];if(roles.length&&!roles.includes(role)){location.href=`${base}${rolePath(role)}`;return;}}
  if(page==='login'&&document.body.dataset.adminLogin==='true'&&session&&role!=='admin'){await api.signOut();document.getElementById('formMessage').textContent='هذه الصفحة مخصصة للإدارة فقط.';document.getElementById('formMessage').className='text-sm text-red-700';return;}
  if((page==='login'||page==='register')&&session){location.href=`${base}${rolePath(role)}`;return;}
  if(page==='profile')await loadProfile(api,session.user.id);
  if(['buyer','seller','admin'].includes(page))await loadDashboard(api,session.user.id,role);
  if(page==='admin')await setupFeeAdmin(api);
  if(page==='marketplace'){await setupProductLocations(api);await setupMarketplace(api,session,role);}
  if(page==='dealView')await loadDealV2(api,session.user,role);
  if(page==='dispute')await loadDisputes(api,session.user,role);
  if(page==='invoice')await loadInvoiceV2(api);
  if(page==='login')setupPasswordReset(api);
  const {initOperations}=await import('./operations.js');
  await initOperations({api,session,role,page,base});
}

async function loadProfile(api,userId){const {data,error}=await api.profiles.mine();const msg=document.getElementById('profileMessage');if(error){msg.textContent=error.message;msg.className='text-sm text-red-700 sm:col-span-2';return;}const form=document.getElementById('profileForm');for(const [key,value] of Object.entries(data||{}))if(form.elements[key])form.elements[key].value=value||'';form.onsubmit=async e=>{e.preventDefault();const button=form.querySelector('button');setBusy(button,true);const raw=Object.fromEntries(new FormData(form));const payload={name:raw.name,email:raw.email||null,national_id:raw.national_id||null,commercial_register:raw.commercial_register||null,iban_bank:raw.iban_bank||null};const {error}=await api.profiles.update(userId,payload);msg.textContent=error?error.message:'تم حفظ البيانات.';msg.className=`text-sm sm:col-span-2 ${error?'text-red-700':'text-emerald-700'}`;setBusy(button,false);};const passwordForm=document.getElementById('passwordForm');passwordForm.onsubmit=async e=>{e.preventDefault();const out=document.getElementById('passwordMessage'),password=new FormData(passwordForm).get('password'),{error}=await api.auth.updatePassword(password);out.textContent=error?error.message:'تم تحديث كلمة المرور.';out.className=`mt-2 text-sm ${error?'text-red-700':'text-emerald-700'}`;if(!error)passwordForm.reset();};document.getElementById('deleteAccountForm').onsubmit=async e=>{e.preventDefault();const out=document.getElementById('deleteMessage'),reason=new FormData(e.target).get('reason');const {error}=await api.profiles.requestDeletion(userId,reason);out.textContent=error?(error.code==='23505'?'لديك طلب حذف قائم بالفعل.':error.message):'تم إرسال طلب حذف الحساب للإدارة.';out.className=`mt-2 text-sm ${error?'text-red-700':'text-emerald-700'}`;if(!error)e.target.querySelector('button').disabled=true;};}

async function loadDashboard(api,userId,role){const el=document.getElementById('dashboardContent');const [{data,error},{data:notes}]=await Promise.all([api.deals.listMine(userId,role),api.notifications.list()]);if(error){el.textContent=error.message;return;}let html=`<h3 class="font-black">الصفقات</h3>${data?.length?`<div class="divide-y">${data.map(d=>`<a class="flex justify-between py-3" href="${base}deal-view/?id=${encodeURIComponent(d.id)}"><span>${escapeHtml(d.product?.title||`صفقة ${d.id.slice(0,8)}`)}</span><span>${money(d.deal_price)} · ${escapeHtml(d.status)}</span></a>`).join('')}</div>`:'<p class="py-4">لا توجد صفقات حتى الآن.</p>'}<h3 class="mt-7 font-black">الإشعارات</h3>${notes?.length?`<div class="divide-y">${notes.map(n=>`<div class="py-3"><p class="font-bold">${escapeHtml(n.title)}</p><p class="text-sm">${escapeHtml(n.body)}</p></div>`).join('')}</div>`:'<p class="py-4">لا توجد إشعارات.</p>'}`;if(role==='seller'){const {data:mine}=await api.products.mine(userId);html+=`<h3 class="mt-7 font-black">منتجاتي</h3>${mine?.length?`<div class="divide-y">${mine.map(p=>`<div class="flex justify-between py-3"><span>${escapeHtml(p.title)} · ${money(p.price)}</span><button data-delete-product="${p.id}" class="text-red-700">حذف</button></div>`).join('')}</div>`:'<p class="py-4">لا توجد منتجات.</p>'}`;}if(role==='admin'){const {data:requests}=await api.admin.deletionRequests();html+=`<h3 class="mt-7 font-black">طلبات حذف الحساب</h3>${requests?.length?requests.map(r=>`<div class="flex flex-wrap justify-between gap-2 border-b py-3"><span>${escapeHtml(r.user?.name)} · ${escapeHtml(r.reason)} · ${escapeHtml(r.status)}</span>${r.status==='pending'?`<span><button data-review="approved" data-id="${r.id}" class="ml-2 text-emerald-700">موافقة</button><button data-review="rejected" data-id="${r.id}" class="text-red-700">رفض</button></span>`:''}</div>`).join(''):'<p class="py-4">لا توجد طلبات.</p>'}`;}el.innerHTML=html;el.onclick=async e=>{if(e.target.dataset.deleteProduct){if(!confirm('حذف المنتج نهائيًا؟'))return;const {error}=await api.products.remove(e.target.dataset.deleteProduct);if(error)alert(error.message);else loadDashboard(api,userId,role);}if(e.target.dataset.review){const {error}=await api.admin.reviewDeletion(e.target.dataset.id,e.target.dataset.review);if(error)alert(error.message);else loadDashboard(api,userId,role);}};}

async function loadDeal(api,user,role){const el=document.getElementById('dealContent'),id=new URLSearchParams(location.search).get('id');if(!id){el.textContent='رابط الصفقة ناقص: لا يوجد رقم صفقة.';return;}const {data:d,error}=await api.deals.get(id);if(error){el.textContent=`تعذر تحميل الصفقة: ${error.message}`;return;}const transitions=[];if(role==='seller'&&d.status==='pending_acceptance')transitions.push(['seller_confirmed','تأكيد قبول الصفقة']);if(role==='buyer'&&d.status==='seller_confirmed')transitions.push(['delivered','تأكيد استلام السلعة']);if(role==='buyer'&&d.status==='delivered')transitions.push(['completed','تأكيد نجاح المعاينة']);if(['buyer','seller'].includes(role)&&['pending_acceptance','seller_confirmed'].includes(d.status))transitions.push(['cancelled','إلغاء الصفقة']);el.innerHTML=`<div class="flex flex-wrap justify-between gap-3"><div><p class="font-bold text-emerald-700">الصفقة ${escapeHtml(d.id.slice(0,8))}</p><h1 class="text-3xl font-black">${escapeHtml(d.product?.title||'صفقة مباشرة')}</h1></div><span class="rounded-full bg-amber-100 px-4 py-2 font-bold text-amber-800">${escapeHtml(d.status)}</span></div><p class="mt-4">المشتري: ${escapeHtml(d.buyer?.name)} — البائع: ${escapeHtml(d.seller?.name)}</p><dl class="mt-6 divide-y rounded-xl border px-4"><div class="flex justify-between py-3"><dt>قيمة الصفقة</dt><dd>${money(d.deal_price)}</dd></div><div class="flex justify-between py-3"><dt>رسوم الضمان</dt><dd>${money(d.escrow_fee)}</dd></div><div class="flex justify-between py-3"><dt>الضريبة</dt><dd>${money(d.vat)}</dd></div><div class="flex justify-between py-3 font-black"><dt>الإجمالي</dt><dd>${money(d.total_amount_paid)}</dd></div></dl><div class="mt-5 flex flex-wrap gap-3">${transitions.map(([status,label])=>`<button data-transition="${status}" class="rounded-xl bg-emerald-700 px-4 py-2 font-bold text-white">${label}</button>`).join('')}<a href="${base}dispute-center/?deal=${d.id}" class="rounded-xl border border-red-300 px-4 py-2 font-bold text-red-700">النزاعات</a>${d.status==='completed'?`<a href="${base}invoice-view/?deal=${d.id}" class="rounded-xl border px-4 py-2 font-bold">الفاتورة</a>`:''}</div><p id="dealActionMessage" class="mt-3 text-sm"></p>`;el.onclick=async e=>{const status=e.target.dataset.transition;if(!status)return;setBusy(e.target,true);const {error}=await api.deals.transition(d.id,status);if(error){document.getElementById('dealActionMessage').textContent=error.message;setBusy(e.target,false);}else loadDeal(api,user,role);};}

async function setupMarketplace(api,session,role){if(!session)return;const toggle=document.getElementById('showProductForm'),form=document.getElementById('productForm'),message=document.getElementById('productMessage'),oldImage=form.elements.main_image;const files=document.createElement('input');files.type='file';files.name='product_images';files.accept='image/jpeg,image/png,image/webp';files.multiple=true;files.required=true;files.className=oldImage.className;oldImage.replaceWith(files);toggle.classList.remove('hidden');toggle.textContent='+ إضافة منتج';toggle.onclick=()=>form.classList.toggle('hidden');form.onsubmit=async e=>{e.preventDefault();const button=form.querySelector('button');setBusy(button,true,'جارٍ رفع المنتج...');const raw=Object.fromEntries(new FormData(form));const selected=[...files.files];if(selected.length>6){message.textContent='الحد الأقصى 6 صور.';setBusy(button,false);return;}if(selected.some(file=>file.size>5242880)){message.textContent='حجم كل صورة يجب ألا يتجاوز 5MB.';setBusy(button,false);return;}const payload={seller_id:session.user.id,title:raw.title,description:raw.description,price:Number(raw.price),category:raw.category,condition:raw.condition,main_image:null,vin:raw.vin||null,location:raw.location||null};const {data:product,error}=await api.products.create(payload);if(error){message.textContent=error.message;message.className='text-sm text-red-700 sm:col-span-2';setBusy(button,false);return;}for(let i=0;i<selected.length;i++){const uploaded=await api.products.uploadImage(session.user.id,product.id,selected[i],i);if(uploaded.error){message.textContent=`أضيف المنتج لكن تعذر رفع صورة: ${uploaded.error.message}`;setBusy(button,false);return;}if(i===0){const {data:image}=await api.products.images(product.id);if(image?.[0])await api.products.update(product.id,{main_image:image[0].public_url});}}message.textContent='تم نشر المنتج وصوره بنجاح.';message.className='text-sm text-emerald-700 sm:col-span-2';setBusy(button,false);form.reset();await initMarket();};}

async function loadDisputes(api,user,role){const dealId=new URLSearchParams(location.search).get('deal'),form=document.getElementById('disputeForm'),list=document.getElementById('disputeList'),message=document.getElementById('disputeMessage');if(!dealId){form.classList.add('hidden');list.textContent='رابط النزاع لا يحتوي رقم الصفقة.';return;}const render=async()=>{const {data,error}=await api.disputes.list(dealId);if(error){list.textContent=error.message;return;}list.innerHTML=data?.length?data.map(d=>`<article class="border-t py-4"><div class="flex justify-between"><strong>${escapeHtml(d.opener?.name)}</strong><span>${escapeHtml(d.status)}</span></div><p class="mt-2">${escapeHtml(d.reason)}</p>${d.admin_response?`<p class="mt-2 rounded-xl bg-slate-50 p-3">رد الإدارة: ${escapeHtml(d.admin_response)}</p>`:''}${role==='admin'&&d.status!=='resolved'?`<form data-resolve="${d.id}" class="mt-3 grid gap-2"><select name="resolution" class="rounded-xl border p-2"><option value="buyer_win">لصالح المشتري</option><option value="seller_win">لصالح البائع</option><option value="split">تقسيم</option></select><textarea name="response" required placeholder="قرار الإدارة" class="rounded-xl border p-2"></textarea><button class="rounded-xl bg-slate-900 p-2 text-white">اعتماد القرار</button></form>`:''}</article>`).join(''):'لا توجد نزاعات على هذه الصفقة.';};form.onsubmit=async e=>{e.preventDefault();const raw=Object.fromEntries(new FormData(form)),attachments=String(raw.attachments||'').split(/\r?\n/).map(x=>x.trim()).filter(Boolean),{error}=await api.disputes.open({deal_id:dealId,opened_by:user.id,reason:raw.reason,attachments});message.textContent=error?error.message:'تم فتح النزاع.';message.className=`text-sm ${error?'text-red-700':'text-emerald-700'}`;if(!error){form.reset();render();}};list.onsubmit=async e=>{const disputeId=e.target.dataset.resolve;if(!disputeId)return;e.preventDefault();const raw=Object.fromEntries(new FormData(e.target)),{error}=await api.disputes.resolve(disputeId,raw.resolution,raw.response);if(error)alert(error.message);else render();};await render();}

async function loadInvoice(api){const el=document.getElementById('invoiceContent'),dealId=new URLSearchParams(location.search).get('deal');if(!dealId){el.textContent='رابط الفاتورة لا يحتوي رقم الصفقة.';return;}const {data:i,error}=await api.invoices.getByDeal(dealId);if(error){el.textContent=error.code==='PGRST116'?'لم تُنشأ فاتورة لهذه الصفقة بعد.':error.message;return;}el.innerHTML=`<div class="flex justify-between border-b pb-5"><div><h1 class="text-3xl font-black text-emerald-800">وجهة</h1><p>فاتورة رسوم الوساطة والضمان</p></div><div class="text-left"><strong>${escapeHtml(i.invoice_number)}</strong><p>${new Date(i.generated_at).toLocaleDateString('ar-SA')}</p></div></div><dl class="mt-6 divide-y"><div class="flex justify-between py-3"><dt>المشتري</dt><dd>${escapeHtml(i.buyer_name)}</dd></div><div class="flex justify-between py-3"><dt>رسوم الضمان</dt><dd>${money(i.total_escrow_fee)}</dd></div><div class="flex justify-between py-3"><dt>ضريبة الرسوم</dt><dd>${money(i.total_vat)}</dd></div><div class="flex justify-between py-3"><dt>صافي مستحق البائع</dt><dd>${money(i.net_payable_to_seller)}</dd></div></dl><button onclick="print()" class="mt-5 rounded-xl bg-emerald-700 px-5 py-3 font-bold text-white print:hidden">طباعة</button>`;}

async function setupProductLocations(api){const form=document.getElementById('productForm');if(!form)return;const box=document.createElement('div');box.className='grid gap-3 sm:col-span-2 sm:grid-cols-3';box.innerHTML='<select name="region_id" required class="rounded-xl border p-3"><option value="">اختر المنطقة</option></select><select name="city_id" required disabled class="rounded-xl border p-3"><option value="">اختر المدينة</option></select><select name="district_id" disabled class="rounded-xl border p-3"><option value="">اختر الحي</option></select>';form.querySelector('textarea').before(box);const region=box.children[0],city=box.children[1],district=box.children[2],{data:regions}=await api.locations.regions();region.innerHTML+=[...(regions||[])].map(x=>`<option value="${x.id}">${escapeHtml(x.name_ar)}</option>`).join('');region.onchange=async()=>{city.disabled=!region.value;district.disabled=true;city.innerHTML='<option value="">اختر المدينة</option>';district.innerHTML='<option value="">اختر الحي</option>';if(region.value){const {data}=await api.locations.cities(region.value);city.innerHTML+=(data||[]).map(x=>`<option value="${x.id}">${escapeHtml(x.name_ar)}</option>`).join('');}};city.onchange=async()=>{district.disabled=!city.value;district.innerHTML='<option value="">اختر الحي</option>';if(city.value){const {data}=await api.locations.districts(city.value);district.innerHTML+=(data||[]).map(x=>`<option value="${x.id}">${escapeHtml(x.name_ar)}</option>`).join('');}};const create=api.products.create;api.products.create=payload=>create({...payload,region_id:Number(region.value),city_id:Number(city.value),district_id:district.value?Number(district.value):null,location:[region.selectedOptions[0]?.text,city.selectedOptions[0]?.text,district.selectedOptions[0]?.text].filter(x=>x&&!x.startsWith('اختر')).join('، ')});}

function setupPasswordReset(api){const button=document.getElementById('forgotPassword');if(!button)return;button.onclick=async()=>{const email=prompt('أدخل البريد الإلكتروني المرتبط بالحساب:');if(!email)return;const {error}=await api.auth.resetPassword(email),msg=document.getElementById('formMessage');msg.textContent=error?error.message:'أرسلنا رابط استعادة كلمة المرور إلى بريدك.';msg.className=`text-sm ${error?'text-red-700':'text-emerald-700'}`;};}

document.addEventListener('submit',async e=>{if(!['loginForm','registerForm'].includes(e.target.id))return;e.preventDefault();const form=e.target,msg=document.getElementById('formMessage'),button=form.querySelector('button[type="submit"],button:not([type])');setBusy(button,true);msg.textContent='جارٍ التحقق...';try{const api=await import('./supabase-client.js'),fd=Object.fromEntries(new FormData(form));if(fd.phone)fd.phone=normalizePhone(fd.phone);const result=form.id==='loginForm'?await api.signIn(fd.email||fd.phone,fd.password):await api.signUp(fd);if(result.error)throw result.error;msg.className='text-sm text-emerald-700';if(result.data.session){let role=result.data.user?.user_metadata?.user_type||'buyer';const {data:profile}=await api.profiles.mine();if(profile?.user_type)role=profile.user_type;if(document.body.dataset.adminLogin==='true'&&role!=='admin'){await api.signOut();throw new Error('هذه الصفحة مخصصة للإدارة فقط.');}msg.textContent='تم الدخول بنجاح، جارٍ نقلك للوحة الحساب...';const next=new URLSearchParams(location.search).get('next');location.href=next&&next.startsWith(location.origin)?next:`${base}${rolePath(role)}`;}else msg.textContent='تم إنشاء الحساب؛ أكّد الجوال أو البريد ثم سجّل الدخول.';}catch(err){msg.className='text-sm text-red-700';msg.textContent=err.message||'تعذر إتمام العملية.';}finally{setBusy(button,false);}});

function setupAdminLoginForm(){const form=document.getElementById('loginForm');if(!form)return;form.innerHTML='<label class="block text-sm font-bold">البريد الإلكتروني للإدارة<input name="email" type="email" dir="ltr" required autocomplete="username" class="mt-2 w-full rounded-xl border p-3"></label><label class="block text-sm font-bold">كلمة المرور<input name="password" type="password" required autocomplete="current-password" class="mt-2 w-full rounded-xl border p-3"></label><button class="w-full rounded-xl bg-slate-900 p-3 font-bold text-white">دخول لوحة الإدارة</button><p id="formMessage" class="text-sm"></p>';}

function applyCommercialCopy(){
  const replacements=new Map([
    ['رسوم الوساطة 2.5% بحد أدنى 50 ر.س ما لم تظهر شروط خاصة قبل اعتماد الصفقة، وتطبق الضريبة على الرسوم فقط.','رسوم خدمة وجهة 5% بحد أدنى 50 ر.س. عند اختيار التقسيط تضاف رسوم 7%. يتحمل كامل الرسوم المشتري أو البائع حسب الاختيار المعروض قبل الاعتماد. لا تُحصّل ضريبة قيمة مضافة حاليًا لأن المنشأة غير مسجلة فيها.'],
    ['إتاحة تابي وتمارا','طلب التقسيط عند توفر مزود معتمد — رسوم 7%']
  ]);
  const walker=document.createTreeWalker(document.getElementById('app'),NodeFilter.SHOW_TEXT);
  while(walker.nextNode())for(const [from,to] of replacements)if(walker.currentNode.nodeValue.includes(from))walker.currentNode.nodeValue=walker.currentNode.nodeValue.replace(from,to);
  if(['terms','escrow','privacy'].includes(page)){
    const article=document.querySelector('article');
    if(article){const notice=document.createElement('section');notice.className='mt-7 rounded-xl border border-amber-200 bg-amber-50 p-4 text-sm leading-7';notice.innerHTML='<strong>إفصاح الدفع والضريبة:</strong> لا توجد حاليًا شراكة أو بوابة دفع/تقسيط مفعلة. لا تحتجز المنصة أي مبلغ قبل الربط بمزود مرخص. رسوم وجهة 5% بحد أدنى 50 ر.س، ورسوم خدمة التقسيط 7% عند اختياره، ويتحملها المشتري أو البائع فقط. المنشأة غير مسجلة في ضريبة القيمة المضافة، ولذلك لا تُحصّل ضريبة حاليًا.';article.append(notice);}
  }
}

function setupFeePreview(){
  const form=document.getElementById('dealWizard');if(!form)return;
  const bearer=form.elements.fee_bearer;
  if(bearer){[...bearer.options].filter(x=>x.value==='split').forEach(x=>x.remove());}
  const price=form.elements.deal_price,installments=form.elements.installments;
  const preview=document.createElement('div');preview.id='feePreview';preview.className='rounded-xl border border-amber-200 bg-emerald-50 p-4 text-sm leading-7';
  const step4=form.querySelector('[data-step="4"]');if(step4)step4.append(preview);
  const render=()=>{const value=Math.max(Number(price?.value)||0,0),platform=Math.max(value*.05,50),bnpl=installments?.checked?value*.07:0,totalFees=platform+bnpl,isBuyer=bearer?.value!=='seller';preview.innerHTML=`<strong>ملخص الرسوم قبل الاعتماد</strong><br>رسوم وجهة (5%، حد أدنى 50 ر.س): ${money(platform)}<br>رسوم التقسيط (7%): ${money(bnpl)}<br>ضريبة القيمة المضافة: غير مطبقة حاليًا<br>يتحمل الرسوم: ${isBuyer?'المشتري':'البائع'}<br><strong>إجمالي دفع المشتري: ${money(value+(isBuyer?totalFees:0))}</strong><br>صافي البائع: ${money(value-(isBuyer?0:totalFees))}${installments?.checked?'<br><span class="text-amber-800">التقسيط طلب مبدئي وغير متاح للدفع حتى اعتماد وربط المزود.</span>':''}`;};
  [price,bearer,installments].filter(Boolean).forEach(x=>x.addEventListener('input',render));render();
}

async function setupFeeAdmin(api){
  const host=document.getElementById('dashboardContent');if(!host)return;
  const {data:s,error}=await api.admin.settings();
  const card=document.createElement('section');card.className='mt-8 rounded-xl border p-5';
  if(error){card.textContent=`تعذر تحميل إعدادات الرسوم: ${error.message}`;host.append(card);return;}
  card.innerHTML=`<h3 class="text-lg font-black">إعدادات الرسوم والدفع</h3><form id="feeSettingsForm" class="mt-4 grid gap-3 sm:grid-cols-2"><label>رسوم وجهة %<input name="fee_percent" type="number" min="0" max="100" step="0.01" value="${Number(s.fee_percent||5)}" class="mt-1 w-full rounded-xl border p-3"></label><label>الحد الأدنى ر.س<input name="minimum_fee" type="number" min="0" step="0.01" value="${Number(s.minimum_fee||50)}" class="mt-1 w-full rounded-xl border p-3"></label><label>رسوم التقسيط %<input name="installment_fee_percent" type="number" min="0" max="100" step="0.01" value="${Number(s.installment_fee_percent||7)}" class="mt-1 w-full rounded-xl border p-3"></label><label>حالة مزود الدفع<input readonly value="${escapeHtml(s.payment_provider_status||'not_connected')}" class="mt-1 w-full rounded-xl border bg-slate-100 p-3"></label><button class="rounded-xl bg-emerald-700 p-3 font-bold text-white sm:col-span-2">حفظ الإعدادات</button><p id="feeSettingsMessage" class="text-sm sm:col-span-2"></p></form><p class="mt-3 text-xs text-slate-600">الضريبة متوقفة حتى تسجيل المنشأة وإضافة الرقم الضريبي. لا تفعّل مزود الدفع قبل العقد والربط الآمن والتحقق من Webhook.</p>`;
  host.append(card);card.querySelector('form').onsubmit=async e=>{e.preventDefault();const raw=Object.fromEntries(new FormData(e.target)),out=card.querySelector('#feeSettingsMessage'),{error}=await api.admin.updateSettings({fee_percent:Number(raw.fee_percent),minimum_fee:Number(raw.minimum_fee),installment_fee_percent:Number(raw.installment_fee_percent)});out.textContent=error?error.message:'تم حفظ إعدادات الرسوم للصفقات الجديدة.';out.className=`text-sm sm:col-span-2 ${error?'text-red-700':'text-emerald-700'}`;};
}

async function loadDealV2(api,user,role){
  const el=document.getElementById('dealContent'),id=new URLSearchParams(location.search).get('id');if(!id){el.textContent='رابط الصفقة ناقص: لا يوجد رقم صفقة.';return;}
  const {data:d,error}=await api.deals.get(id);if(error){el.textContent=`تعذر تحميل الصفقة: ${error.message}`;return;}
  const transitions=[];if(role==='seller'&&d.status==='pending_acceptance')transitions.push(['seller_confirmed','تأكيد قبول الصفقة']);if(role==='buyer'&&d.status==='seller_confirmed')transitions.push(['delivered','تأكيد استلام السلعة']);if(role==='buyer'&&d.status==='delivered')transitions.push(['completed','تأكيد نجاح المعاينة']);if(['buyer','seller'].includes(role)&&['pending_acceptance','seller_confirmed'].includes(d.status))transitions.push(['cancelled','إلغاء الصفقة']);
  el.innerHTML=`<div class="flex flex-wrap justify-between gap-3"><div><p class="font-bold text-emerald-700">الصفقة ${escapeHtml(d.id.slice(0,8))}</p><h1 class="text-3xl font-black">${escapeHtml(d.product?.title||'صفقة مباشرة')}</h1></div><span class="rounded-full bg-amber-100 px-4 py-2 font-bold text-amber-800">${escapeHtml(d.status)}</span></div><p class="mt-4">المشتري: ${escapeHtml(d.buyer?.name)} — البائع: ${escapeHtml(d.seller?.name)}</p><dl class="mt-6 divide-y rounded-xl border px-4"><div class="flex justify-between py-3"><dt>قيمة الصفقة</dt><dd>${money(d.deal_price)}</dd></div><div class="flex justify-between py-3"><dt>رسوم وجهة (${Number(d.fee_percent_snapshot||5)}%)</dt><dd>${money(d.escrow_fee)}</dd></div><div class="flex justify-between py-3"><dt>رسوم التقسيط</dt><dd>${money(d.installment_fee||0)}</dd></div><div class="flex justify-between py-3"><dt>ضريبة القيمة المضافة</dt><dd>${d.vat_applied?money(d.vat):'غير مطبقة'}</dd></div><div class="flex justify-between py-3"><dt>يتحمل الرسوم</dt><dd>${d.fee_bearer==='seller'?'البائع':'المشتري'}</dd></div><div class="flex justify-between py-3 font-black"><dt>إجمالي دفع المشتري</dt><dd>${money(d.total_amount_paid)}</dd></div><div class="flex justify-between py-3"><dt>صافي البائع</dt><dd>${money(d.seller_net??d.deal_price)}</dd></div></dl>${d.installment_enabled?'<p class="mt-4 rounded-xl bg-amber-50 p-4 text-sm text-amber-800">اختيار التقسيط مسجل، لكن الدفع به غير متاح حتى اعتماد وربط مزود رسمي.</p>':''}<div class="mt-5 flex flex-wrap gap-3">${transitions.map(([status,label])=>`<button data-transition="${status}" class="rounded-xl bg-emerald-700 px-4 py-2 font-bold text-white">${label}</button>`).join('')}<a href="${base}dispute-center/?deal=${encodeURIComponent(d.id)}" class="rounded-xl border border-red-300 px-4 py-2 font-bold text-red-700">النزاعات</a>${d.status==='completed'?`<a href="${base}invoice-view/?deal=${encodeURIComponent(d.id)}" class="rounded-xl border px-4 py-2 font-bold">المستند المالي</a>`:''}</div><p id="dealActionMessage" class="mt-3 text-sm"></p>`;
  el.onclick=async e=>{const status=e.target.dataset.transition;if(!status)return;setBusy(e.target,true);const {error}=await api.deals.transition(d.id,status);if(error){document.getElementById('dealActionMessage').textContent=error.message;setBusy(e.target,false);}else loadDealV2(api,user,role);};
}

async function loadInvoiceV2(api){
  const el=document.getElementById('invoiceContent'),dealId=new URLSearchParams(location.search).get('deal');if(!dealId){el.textContent='رابط المستند لا يحتوي رقم الصفقة.';return;}
  const {data:i,error}=await api.invoices.getByDeal(dealId);if(error){el.textContent=error.code==='PGRST116'?'لم يُنشأ مستند مالي لهذه الصفقة بعد.':error.message;return;}
  el.innerHTML=`<div class="flex justify-between border-b pb-5"><div><h1 class="text-3xl font-black text-emerald-800">وجهة</h1><p>${i.is_tax_invoice?'فاتورة ضريبية':'فاتورة/مستند رسوم غير ضريبي'}</p></div><div class="text-left"><strong>${escapeHtml(i.invoice_number)}</strong><p>${new Date(i.generated_at).toLocaleDateString('ar-SA')}</p></div></div>${!i.is_tax_invoice?'<p class="mt-5 rounded-xl bg-amber-50 p-4 text-sm">المنشأة غير مسجلة في ضريبة القيمة المضافة؛ هذا المستند ليس فاتورة ضريبية ولم تُحصّل ضريبة.</p>':''}<dl class="mt-6 divide-y"><div class="flex justify-between py-3"><dt>المشتري</dt><dd>${escapeHtml(i.buyer_name)}</dd></div><div class="flex justify-between py-3"><dt>رسوم وجهة</dt><dd>${money(i.total_escrow_fee)}</dd></div><div class="flex justify-between py-3"><dt>رسوم التقسيط</dt><dd>${money(i.installment_fee||0)}</dd></div>${i.is_tax_invoice?`<div class="flex justify-between py-3"><dt>ضريبة القيمة المضافة</dt><dd>${money(i.total_vat)}</dd></div>`:''}<div class="flex justify-between py-3"><dt>صافي مستحق البائع</dt><dd>${money(i.net_payable_to_seller)}</dd></div></dl><button onclick="print()" class="mt-5 rounded-xl bg-emerald-700 px-5 py-3 font-bold text-white print:hidden">طباعة</button>`;
}
